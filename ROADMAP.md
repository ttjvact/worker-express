# worker-express ロードマップ（バージョン別タスク管理）

## 目的
- このドキュメントは、**各バージョンで実施するタスク一覧と完了条件**を管理する。

---

## 運用ルール
- `0.1.0` を MVP とする。
- 実装前タスクは本ファイルに記載し、実施後に `- [x]` へ更新する。
- `x.y.z` の **z（パッチ）系バージョンは、明示指示があるまでバージョン更新しない**。
  - 例: `0.2.1` の想定タスクを対応した場合、まずは本ファイル上でチェック更新のみ行う。
- リリース実施時のみ `CHANGELOG.md` を更新する。
- HTTP 応答仕様は Express 互換を優先し、`405 Method Not Allowed` はコアの標準応答に含めない。

---

## 0.0.1（コア実装）
- Cloudflare Workers 上で Express 風 API の最小セットを実装する。

1. 基盤セットアップ
- [x] package 初期化
- [x] TypeScript ビルド構成（`dist/index.js` と `dist/index.d.ts`）
- [x] lint / test の実行基盤

2. 最小 API 実装
- [x] `express()`
- [x] `app.get/post/put/patch/delete(path, ...handlers)`
- [x] `res.status()`, `res.set()`, `res.send()`, `res.json()`, `res.end()`
- [x] `app.fetch(request, env, ctx)`

3. ルーティング/ミドルウェア
- [x] `app.use()`
- [x] `next()`
- [x] path params（`/users/:id`）
- [x] `req.query`

4. エラーパス
- [x] `next(err)` による 500 応答

完了条件
- [x] README の最小サンプル相当が動作する。
- [x] テストが通る。

---

## 0.0.2（運用・ドキュメント整備）
- MVP 前提で利用者と開発者向けの基本ドキュメントを整える。

1. ドキュメント責務の明確化
- [x] `ROADMAP.md` を「バージョン別タスク管理」に再編
- [x] `AGENTS.md` に「ルール集約先」の位置づけを明記
- [x] `CHANGELOG.md` はリリース履歴に限定する方針を明記

2. 詳細ドキュメント追加
- [x] `docs/` 配下に詳細な使い方ガイドを追加
- [x] README から `docs/` への導線追加

3. 進行管理ルール
- [x] patch バージョン（z）運用ルールを本ファイルへ明記

完了条件
- [ ] 新規参加者がリポジトリ上の README + docs を見て、ローカルで利用手順を再現できる。

---

## 0.0.3（公開準備の土台）
- `0.1.0` の公開判定に必要な公開手順と配布前チェックを先に整える。

1. 公開前提の整備
- [ ] `oss_mvp_checklist.md` に従って、公開前に必要な Git / GitHub / npm の準備を進める
- [ ] npm 公開に必要な確認事項を `oss_mvp_checklist.md` で管理する
- [ ] checklist の結果を踏まえて、公開前に不足している作業を埋める

2. 公開品質の見える化
- [x] CI 設定（Node LTS matrix）を追加
- [ ] CI 緑化を公開判定条件として固定

完了条件
- [ ] `oss_mvp_checklist.md` を基準に `0.1.0` の公開判定に必要な手順と前提確認が揃っている。

---

## 0.1.0（MVP リリース）
- Cloudflare Workers 上で Express 風 API の最小セットを提供する。

1. リリース前の最終整備
- [ ] README 整備（互換レベル表の拡充と Express 差分の明記）
- [ ] npm 配布物でも有効なドキュメント導線を確定
- [ ] `npm publish --access public` 実施

2. MVP 判定
- [ ] Cloudflare Workers 上で Express 風 API の最小セットを公開可能な形で提供する
- [ ] 0.0.x で定義した実装・ドキュメント・公開準備タスクを反映した状態でリリースする

完了条件
- [ ] README の最小サンプル相当が npm 配布物でも再現できる。
- [ ] publish 可能な品質ゲートを満たす。
- [ ] `CHANGELOG.md` に `0.1.0` のリリース内容を記録できる状態にする。

---

## 0.1.1（fallthrough の Express ライク化）
- ルート到達後に未送信で終了した場合の既定応答を Express ライクな 404 へ寄せる。

1. fallthrough 応答の見直し
- [ ] ルート一致後に `next()` だけで応答未送信の場合、既定で 404 を返すよう修正
- [ ] 既存の 204 前提テストを Express ライクな期待値へ更新
- [ ] README / docs の未送信時挙動を明記

完了条件
- [ ] 未送信 fallthrough の既定挙動が 404 で固定され、テストとドキュメントが一致している。

---

## 0.1.2（`headersSent` の整合化）
- 送信確定後にレスポンス状態が変化しないよう、`headersSent` と内部状態遷移を合わせる。

1. 状態遷移の修正
- [ ] `send/json/end` 後に `status/set` で状態が変わらないよう修正
- [ ] `headersSent` の意味を「以降の変更不可」と一致させる
- [ ] 状態遷移に関する境界値テストを追加

完了条件
- [ ] 送信確定後のレスポンス変更規則が実装とテストで固定されている。

---

## 0.1.3（`content-type` 上書き規則の固定）
- `res.set` と `res.json` の優先関係を仕様として明記し、挙動を固定する。

1. ヘッダ優先順位の明文化
- [ ] `res.json()` 実行時の `content-type` 上書き規則を README / docs / API 仕様表へ明記
- [ ] 実装を仕様どおりに固定
- [ ] `content-type` を事前設定した場合のテストを追加

完了条件
- [ ] `content-type` の上書き規則が利用者向け仕様として一意に読める。

---

## 0.1.4（`res.end` の低レベル化）
- `res.end` を `send` の別名ではなく、より低レベルなレスポンス終了 API として整理する。

1. `res.end` の仕様修正
- [ ] `res.end` が本文変換や `content-type` 補完を暗黙に行わないよう見直す
- [ ] `res.send` / `res.end` の責務差分を README / docs に明記
- [ ] 低レベル API としての `res.end` テストを追加

完了条件
- [ ] `res.end` の責務が `res.send` と明確に分離されている。

---

## 0.2.0（互換性・品質向上）
- MVP 後の互換性確認と回帰検知を強化する。

1. テスト拡充
- [ ] Miniflare / Wrangler ベースの結合テスト追加
- [ ] middleware 実行順序の網羅ケース追加
- [ ] エラー処理（`next(err)`）の境界値テスト追加

2. 品質管理
- [ ] 型チェックを CI に組み込む
- [ ] 主要 API の破壊的変更検知ルール策定

完了条件
- [ ] 主要 API の回帰バグを CI で捕捉できる状態にする。

---

## 0.3.0（開発者体験の向上）
1. 利用ガイド拡充
- [ ] ユースケース別サンプル（API、middleware、error handler）を docs に追加
- [ ] トラブルシュートを docs に追加

2. 開発効率
- [ ] ローカル検証テンプレート（example 拡充）
- [ ] 変更時チェックリストを docs 化

完了条件
- [ ] 初回利用者が docs のみで一般的な API を構築できる。

---

## 0.4.0（配布・運用基盤）
1. リポジトリ運用
- [ ] GitHub Actions で release フロー整備
- [ ] 変更履歴の更新手順を定義

2. メンテナンス運用
- [ ] 定期リリース時の運用手順を docs 化

完了条件
- [ ] メンテナが再現可能な運用手順を持つ。

---

## 0.5.0（拡張 API: ルーティング）
1. 追加 API
- [ ] `express.Router()`
- [ ] `app.route()`

2. 互換性確認
- [ ] 既存 API との後方互換テスト追加

完了条件
- [ ] ルータ分割を使った中規模構成に対応できる。

---

## 0.6.0（拡張 API: 実運用補助）
1. helper 系
- [ ] cookie helper
- [ ] CORS helper

2. ドキュメント
- [ ] helper 利用時の推奨設定を docs 化

完了条件
- [ ] 実運用向けの基本補助機能が提供される。

---

## 0.7.0（静的配信・制約整理）
1. 機能
- [ ] static 配信（Workers 制約前提）

2. ドキュメント
- [ ] Workers 制約と代替構成を docs に明記

完了条件
- [ ] static 配信の適用範囲と制約が明確である。

---

## 0.8.0（1.0.0 前最終安定化）
1. 安定化
- [ ] パフォーマンス測定（簡易ベンチ）
- [ ] 主要 API の挙動凍結（breaking change 候補の解消）
- [ ] ドキュメント最終整備

2. 運用確認
- [ ] リリース手順のリハーサル

完了条件
- [ ] 1.0.0 に向けた blocking issue が解消されている。

---

## 1.0.0（安定版）
- 0.x 系で定義した必須機能・品質・運用要件を満たし、安定版として公開する。

1. リリース判定
- [ ] API 仕様の安定宣言
- [ ] ドキュメントの完成
- [ ] CI / テスト / リリースフローの固定化

完了条件
- [ ] 安定版として利用者に案内可能な品質である。
