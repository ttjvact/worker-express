{
  "name": "worker-express",
  "version": "0.1.0",
  "description": "Express-like routing for Cloudflare Workers",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test",
    "lint": "node scripts/lint.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "types": "dist/index.d.ts",
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
