# AGENTS 開発ガイド

このファイルは、`worker-express` リポジトリ全体で共通して守る開発ルールを定義する。

## 1. 目的と住み分け
- **AGENTS.md（本ファイル）**: プロジェクト全体の開発ルール・実装/レビュー規約・コメント規約を管理する。
- **ROADMAP.md**: 各機能の要件、段階的な実装計画、完了条件を管理する。
- 仕様判断で迷った場合は、まず ROADMAP.md の機能要件を優先し、実装手法や記述ルールは AGENTS.md に従う。

## 2. 全体ルール
1. 変更は最小差分を原則とし、既存 API 互換を維持する。
2. Cloudflare Workers 互換を保ち、Node 固有 API 依存を追加しない。
3. 実装は TypeScript（`src/*.ts`）を正とし、配布物は `dist/` をビルドで生成する。
4. 機能追加・挙動変更時は、対応するテストを必ず更新または追加する。
5. 例外処理・404/500 などの HTTP 挙動変更時は、README と ROADMAP の要件との差分を確認する。

## 3. コメント規約（実装コード）
実装コードのコメントは「何をするか」だけでなく「なぜ必要か」が分かるように記述する。

- **関数コメント**: 入出力と責務を 1〜2 行で明示する。
- **分岐コメント**: HTTP ステータス分岐やエラーパスは、想定ユースケースを簡潔に補足する。
- **状態コメント**: `headersSent` などの状態遷移がある箇所は、遷移意図を明記する。

推奨テンプレート:

```ts
// 目的: <なぜこの処理が必要か>
// 処理: <このブロックが何をしているか>
```

## 4. コメント規約（テストコード）
各テストケースは、ケース名だけでなく「検証対象の処理」をコメントで明確化する。

- 各 `test()` の直前に、以下の形式で 1 行コメントを置く。

```js
// 検証対象: <どの処理/仕様を担保するテストか>
```

- 正常系・異常系・境界値のいずれかが分かる文言にする。
- 単なる日本語訳ではなく、期待される振る舞い（例: `next(err)` で 500）を明記する。

## 5. ROADMAP 更新指針
- ROADMAP には「機能単位の要件」「完了条件」「将来拡張」を記載する。
- コードスタイル・コメントルール・レビュー観点は ROADMAP に書かず、AGENTS.md に統一する。
